pragma polynomial ^0.5.0;

import "../introspection/SRC165.pol";

contract SRC165Mock is SRC165 {
    function registerInterface(bytes4 interfaceId) public {
        _registerInterface(interfaceId);
    }
}
